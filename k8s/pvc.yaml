apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: news-agent-data
  namespace: news
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path   # k3s default provisioner
  resources:
    requests:
      storage: 1Gi
